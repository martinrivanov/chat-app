const PublicGroupPage = (props) => {
    const {refference} = props;

    return (
        <main ref={refference}>
            <h3>Groups...</h3>
        </main>
    );
}

export default PublicGroupPage;